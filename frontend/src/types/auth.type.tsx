export type LoginForm = {
  username: string;
};

export type RegisterForm = {
  name: string;
  username: string;
  preferredTimezone: string;
};

export type UserType = {
  id: string;
  name: string;
  username: string;
  preferredTimezone: string;
};
